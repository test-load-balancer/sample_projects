<ivysettings>
    <settings defaultResolver="tlb_sample"/>
    <property name="ivy.default.settings.dir" value="jar:file://${ivy.jar.file}!/org/apache/ivy/core/settings" override="true"/>
    <include url="${ivy.default.settings.dir}/ivysettings-public.xml"/>
    <include url="${ivy.default.settings.dir}/ivysettings-shared.xml"/>
    <include url="${ivy.default.settings.dir}/ivysettings-local.xml"/>
    <include url="${ivy.default.settings.dir}/ivysettings-main-chain.xml"/>
    <include url="${ivy.default.settings.dir}/ivysettings-default-chain.xml"/>
    <resolvers>
        <chain name="self-dev">
            <filesystem name="local">
                <artifact pattern="${basedir}/../../tlb/target/[artifact]-[revision].[ext]"/>
            </filesystem>
        </chain>
        <chain name="local-dev">
            <filesystem name="local">
                <artifact pattern="${basedir}/../../tlb/lib/prod/[artifact]-[revision].[ext]"/>
            </filesystem>
        </chain>
        <chain name="self-dist">
            <filesystem name="local">
                <artifact pattern="${basedir}/../../[artifact]-[revision].[ext]"/>
            </filesystem>
        </chain>
        <chain name="local-dist">
            <filesystem name="local">
                <artifact pattern="${basedir}/../../lib/[artifact]-[revision].[ext]"/>
            </filesystem>
        </chain>
        <chain name="tlb_sample">
            <resolver ref="self-dev"/>
            <resolver ref="local-dev"/>
            <resolver ref="self-dist"/>
            <resolver ref="local-dist"/>
            <resolver ref="default"/>
        </chain>
    </resolvers>
</ivysettings>